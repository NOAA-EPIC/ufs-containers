#!/bin/bash
set -x
export SINGULARITYENV_FI_PROVIDER=tcp
export SINGULARITY_SHELL=/bin/bash
img="IMAGE"
cmd=$(basename "$0")
arg="$@"
echo running: singularity exec "${img}" $cmd $arg
PATH_TO_SINGULARITY exec -B /BINDDIR -B /LOCDIR -B /ROCOTODIR --env-file SRW_ENV "${img}" $cmd $arg

